name: Build Database

on:
  push:
    branches: [ "data-pipeline" ]

jobs:

  build:
    runs-on: [ubuntu-latest]

    steps:
    - uses: actions/checkout@v4
    - name: Test
      run: echo "testing"
